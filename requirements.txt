# Core
httpx>=0.27,<1

# API server
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# Memory / RAG
sentence-transformers>=2.2
faiss-cpu>=1.7

# Voice (optional — install for voice mode)
faster-whisper>=1.0
TTS>=0.22
sounddevice>=0.4
numpy>=1.24

# Vision (optional — install for screen capture/OCR)
mss>=9.0
Pillow>=10.0
pytesseract>=0.3

# Dev / test (optional)
pytest>=8,<9
